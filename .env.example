# Trustee App environment example

APP_ENV=production
FLASK_APP=app.py
FLASK_DEBUG=0

# Replace with a long random value in real deployment
SECRET_KEY=REPLACE_WITH_LONG_RANDOM_SECRET_KEY

# Optional runtime port if your host uses it
PORT=5000
