# ADR-7 — Canonical Navigation Graph

Defines one canonical navigation tree for IOS.

Core rule: every route must belong to one primary workspace and may only appear elsewhere as a shortcut, not a duplicate owner.

Workspaces:
HOME → CREATE → ADMINISTER → PEOPLE → GOVERNANCE → COMPLIANCE → LEGACY → LIBRARY → RESEARCH → ARCHIVE → REPORTS → SYSTEM → DEVELOPER

Open items:
- Resolve `/trusts`
- Resolve `/funding`
- Resolve `/trust-minutes`
- Resolve `/matter-relationships`
- Regenerate ADR-5 because all routes fell into REVIEW.
